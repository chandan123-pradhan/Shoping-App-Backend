class Products {
    constructor(userId,productName,productDescription,oldPrice,newPrice,noOfItem,images) {
       this.userId,
       this.productName,
       this.productDescription,
       this.newPrice,
       this.oldPrice
       this.noOfItem,
       this.images
    }
}